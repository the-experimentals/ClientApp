import { NavMenuDirective } from './nav-menu.directive';

describe('NavMenuDirective', () => {
  it('should create an instance', () => {
    const directive = new NavMenuDirective();
    expect(directive).toBeTruthy();
  });
});
